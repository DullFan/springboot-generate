/**
 * tdesign v1.9.4
 * (c) 2024 tdesign
 * @license MIT
 */

import { _ as _defineProperty } from '../_chunks/dep-2dead3b8.mjs';
import { ref } from 'vue';
import { i as isUndefined_1 } from '../_chunks/dep-fb07e55c.mjs';
import { i as isNumber_1 } from '../_chunks/dep-844e471b.mjs';
import { i as isObject_1 } from '../_chunks/dep-a053ff3d.mjs';
import { i as isArray_1 } from '../_chunks/dep-4ef63f67.mjs';
import { calcSize } from '../utils/responsive.mjs';
import { useListener } from '../hooks/useListener.mjs';
import { isServer } from '../utils/dom.mjs';
import '../_chunks/dep-8a6a0750.mjs';
import '../_chunks/dep-ad0f5c12.mjs';
import '../_chunks/dep-de0d9668.mjs';
import '../_chunks/dep-c23362d5.mjs';
import '../_chunks/dep-d5cc8b5d.mjs';
import '../_chunks/dep-f5bde8c1.mjs';
import '../_chunks/dep-706c37a3.mjs';
import '../_chunks/dep-65c48aa3.mjs';
import '../utils/easing.mjs';

function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function useRowSize() {
  var size = ref(calcSize(isServer ? 0 : window.innerWidth));
  var updateSize = function updateSize() {
    size.value = calcSize(isServer ? 0 : window.innerWidth);
  };
  useListener("resize", updateSize);
  return size;
}
function getRowClasses(name, props) {
  var justify = props.justify,
    align = props.align;
  return [name, _defineProperty(_defineProperty({}, "".concat(name, "--").concat(justify), justify), "".concat(name, "--align-").concat(align), align)];
}
function calcRowStyle(gutter, currentSize) {
  var rowStyle = {};
  var getMarginStyle = function getMarginStyle(gutter2) {
    return Object.assign(rowStyle, {
      marginLeft: "".concat(gutter2 / -2, "px"),
      marginRight: "".concat(gutter2 / -2, "px")
    });
  };
  var getRowGapStyle = function getRowGapStyle(gutter2) {
    return Object.assign(rowStyle, {
      rowGap: "".concat(gutter2, "px")
    });
  };
  var strategyMap = {
    isNumber: function isNumber(gutter2) {
      if (isNumber_1(gutter2)) {
        getMarginStyle(gutter2);
      }
    },
    isArray: function isArray(gutter2) {
      if (isArray_1(gutter2) && gutter2.length) {
        strategyMap.isNumber(gutter2[0]);
        if (isNumber_1(gutter2[1])) {
          getRowGapStyle(gutter2[1]);
        }
        if (isObject_1(gutter2[0]) && !isUndefined_1(gutter2[0][currentSize])) {
          getMarginStyle(gutter2[0][currentSize]);
        }
        if (isObject_1(gutter2[1]) && !isUndefined_1(gutter2[1][currentSize])) {
          getRowGapStyle(gutter2[1][currentSize]);
        }
      }
    },
    isObject: function isObject(gutter2) {
      if (isObject_1(gutter2) && gutter2[currentSize]) {
        if (isArray_1(gutter2) && gutter2.length) {
          getMarginStyle(gutter2[currentSize][0]);
          getRowGapStyle(gutter2[currentSize][1]);
        } else {
          getMarginStyle(gutter2[currentSize]);
        }
      }
    }
  };
  Object.keys(strategyMap).forEach(function (item) {
    strategyMap[item](gutter);
  });
  return rowStyle;
}
function parseFlex(flex) {
  if (isNumber_1(flex)) {
    return "".concat(flex, " ").concat(flex, " 0");
  }
  if (/^\d+(\.\d+)?(px|r?em|%)$/.test(flex)) {
    return "0 0 ".concat(flex);
  }
  return flex;
}
function calcColPadding(gutter, currentSize) {
  var paddingObj = {};
  var getPaddingStyle = function getPaddingStyle(gutter2) {
    return Object.assign(paddingObj, {
      paddingLeft: "".concat(gutter2 / 2, "px"),
      paddingRight: "".concat(gutter2 / 2, "px")
    });
  };
  var strategyMap = {
    isNumber: function isNumber(gutter2) {
      if (isNumber_1(gutter2)) {
        getPaddingStyle(gutter2);
      }
    },
    isArray: function isArray(gutter2) {
      if (isArray_1(gutter2) && gutter2.length) {
        if (isNumber_1(gutter2[0])) {
          getPaddingStyle(gutter2[0]);
        }
        if (isObject_1(gutter2[0]) && gutter2[0][currentSize]) {
          getPaddingStyle(gutter2[0][currentSize]);
        }
      }
    },
    isObject: function isObject(gutter2) {
      if (isObject_1(gutter2) && gutter2[currentSize]) {
        getPaddingStyle(gutter2[currentSize]);
      }
    }
  };
  Object.keys(strategyMap).forEach(function (item) {
    strategyMap[item](gutter);
  });
  return paddingObj;
}
function getColClasses(name, props) {
  var span = props.span,
    order = props.order,
    offset = props.offset,
    push = props.push,
    pull = props.pull;
  var allSizes = ["xs", "sm", "md", "lg", "xl", "xxl"];
  var ColSizeClasses = allSizes.reduce(function (acc, currSize) {
    var sizeProp = props[currSize];
    var sizeObject = {};
    if (isNumber_1(sizeProp)) {
      sizeObject.span = sizeProp;
    } else if (isObject_1(sizeProp)) {
      sizeObject = sizeProp || {};
    }
    return _objectSpread(_objectSpread({}, acc), {}, _defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty({}, "".concat(name, "-").concat(currSize, "-").concat(sizeObject.span), !isUndefined_1(sizeObject.span)), "".concat(name, "-").concat(currSize, "-order-").concat(sizeObject.order), parseInt(sizeObject.order, 10) >= 0), "".concat(name, "-").concat(currSize, "-offset-").concat(sizeObject.offset), parseInt(sizeObject.offset, 10) >= 0), "".concat(name, "-").concat(currSize, "-push-").concat(sizeObject.push), parseInt(sizeObject.push, 10) >= 0), "".concat(name, "-").concat(currSize, "-pull-").concat(sizeObject.pull), parseInt(sizeObject.pull, 10) >= 0));
  }, {});
  return _objectSpread(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty({}, "".concat(name), true), "".concat(name, "-").concat(span), !isUndefined_1(span)), "".concat(name, "-order-").concat(order), order), "".concat(name, "-offset-").concat(offset), offset), "".concat(name, "-push-").concat(push), push), "".concat(name, "-pull-").concat(pull), pull), ColSizeClasses);
}

export { calcColPadding, calcRowStyle, getColClasses, getRowClasses, parseFlex, useRowSize };
//# sourceMappingURL=common.mjs.map
